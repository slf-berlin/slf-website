{
  "name": "slf-berlin",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "sync": "node scripts/sync-from-wordpress.mjs",
    "mirror": "node scripts/mirror-wp-media.mjs",
    "backup": "npm run sync && npm run mirror",
    "prebuild": "node scripts/sync-from-wordpress.mjs && node scripts/mirror-wp-media.mjs && node scripts/generate-sitemap.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.30.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "gh-pages": "^6.3.0",
    "playwright": "^1.60.0",
    "vite": "^6.3.5",
    "yaml": "^2.9.0"
  }
}
